<%- include("partials/header") %>

<h1>This is the campgrounds page!</h1>

<ul>
  <% campgrounds.map(c => { %>
  <div>
    <h4><%= c.name %></h4>
     <img src="<%= c.image %>"">
  </div>
  <% }) %>
</ul>

<%- include("partials/footer") %>